HTML validation service
W3C XHTML Validator
The Markup Validator is a free service by W3C that helps check the validity of Web documents. It checks the syntax of Web documents, written in formats such as (X)HTML. The API uses SOAP protocol and responses are formatted in XML.
html internet webdesign 
Internet
